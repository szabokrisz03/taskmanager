<MudNavMenu>
    <MudNavLink Href="mytasks" Disabled="true">Saját feladatok</MudNavLink>

    @if (projectTechnicalName != null){
        <MudNavGroup Title="@projectName" Expanded="true">
            <MudNavLink Href="@($"projects/{projectTechnicalName}/tasks")">Feladatok</MudNavLink>
            <MudNavLink Href="@($"projects/{projectTechnicalName}/milestones")" Disabled="true">Mérföldkövek</MudNavLink>
            <MudNavLink Href="@($"projects/{projectTechnicalName}/witemplates")" Disabled="true">WI Sablonok</MudNavLink>
        </MudNavGroup>
    }
</MudNavMenu>
